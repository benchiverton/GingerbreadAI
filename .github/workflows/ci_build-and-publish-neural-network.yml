name: Build and Publish Neural Network

on:
  push:
    branches: [ master ]
    paths:
    - 'src/Model/GingerbreadAI.Model.NeuralNetwork/**'
    - '.github/**'
  pull_request:
    branches: [ master ]
    paths:
    - 'src/Model/GingerbreadAI.Model.euralNetwork/**'
    - '.github/**'

env:
  PROJECT_LOCATION: src/Model/GingerbreadAI.Model.NeuralNetwork/
  PROJECT_FILE: GingerbreadAI.Model.NeuralNetwork.csproj

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.1.101
        source-url: https://nuget.pkg.github.com/benchiverton/index.json
      env:
        NUGET_AUTH_TOKEN: ${{secrets.GITHUB_TOKEN}}
    - name: Copy Icon
      run: cp -v docs/Images/icon.png $PROJECT_LOCATION
      # suffix the package with the run number if building from a PR
    - name: Pack Pre-Release
      if: github.event_name == 'pull_request' && github.event.action != 'closed'
      env:
        RUN_NUMBER: ${{ github.run_number }}
        working-directory: $PROJECT_LOCATION
      run: dotnet pack $PROJECT_FILE --configuration Release --version-suffix $RUN_NUMBER --include-symbols --no-dependencies
    - name: Pack Release
      if: github.event_name == 'push'
      working-directory: $PROJECT_LOCATION
      run: dotnet pack $PROJECT_FILE --configuration Release --include-symbols --no-dependencies
    - name: Publish
      working-directory: $PROJECT_LOCATION
      run: dotnet nuget push bin/Release/*.nupkg --skip-duplicate